package wine.builtin.libXext;

import wine.loader.BuiltinModule;
import wine.system.WineProcess;

public class LibXext extends BuiltinModule {
    public LibXext(String name, WineProcess process, int id) {
        super(name, process, id);
    }
}
